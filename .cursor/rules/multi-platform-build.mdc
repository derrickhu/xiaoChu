---
description: 多平台打包配置规则 - 项目支持微信/抖音等多平台发布，打包配置有特定的维护流程
globs: "**/project.config.json,**/config/*.project.config.json,**/game.json,**/game.js"
alwaysApply: false
---

# 多平台打包配置

## 配置文件架构

项目通过 `config/` 目录管理各平台的打包配置源文件：

- `config/wechat.project.config.json` — 微信平台**源配置**
- `config/douyin.project.config.json` — 抖音平台源配置（如有）
- `project.config.json`（根目录）— 当前平台的**运行时副本**，由构建流程从 `config/` 复制而来

## 修改规则

1. **永远先改 `config/` 下的源文件**，再同步到根目录的 `project.config.json`
2. **不要只改 `project.config.json`**，它会在下次构建时被覆盖
3. 修改 `packOptions.ignore`（排除文件夹/文件）时，确认源文件和副本保持一致

## 分包配置

分包涉及两个文件，需同步修改：

- `game.json` — 声明 `subpackages` 数组（微信编译器读取）
- `game.js` — `_subPkgNames` 数组（运行时加载分包用）

每个子包目录下需要一个空的 `game.js` 入口文件（微信编译器要求）。

## 排除文件（不打包）

如果某个目录不需要打进任何包，在 `packOptions.ignore` 中添加即可，**不需要**配置为子包。

## 抖音上线分支与兼容原则

- 抖音上线/适配工作使用独立分支 `platform/douyin`。
- 抖音专用改动应尽量隔离在平台配置、`js/platform.js` 或小型适配层中，避免污染通用业务逻辑。
- 修改平台 API 相关逻辑时，必须同时考虑微信与抖音：优先能力检测和降级处理，不要直接假设 `wx` 或 `tt` 一定存在。
- 除非明确要求，抖音适配不能破坏微信端现有入口、分享、广告、云能力、分包和资源加载流程。
