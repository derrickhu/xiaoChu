# 经济体系 & 数值改动规范

## 全盘考虑原则

涉及经济体系（灵石、碎片、觉醒石、经验）和数值平衡（怪物属性、奖励公式、成长曲线）的改动，**必须**全盘考虑以下维度后再动工：

1. **日产出预算对齐**：对照 `ECONOMY_FRAMEWORK.dailyTarget` 和 `sourceRatio`，评估改动后该来源的日产出是否仍在预算范围内。
2. **上下游公式一致**：改动某一处公式（如每层灵石）时，必须同步检查并更新关联公式（如总结算灵石、每日任务奖励等），确保数据自洽。
3. **击杀经验完整性**：任何新增的击杀路径（技能、武器、DoT、反击等）都必须调用 `addKillExp(g)`，否则会导致经验和灵石计算偏差。
4. **难度递进感**：奖励公式应体现层数/章节的难度递进，避免使用不随进度增长的固定常量。
5. **防无限产出**：新增产出渠道时，必须设置每日/每周上限（参考 `TOWER_DAILY`、`AD_REWARDS.dailyLimit`）。

## 核心配置文件

改动以下文件时需格外谨慎，并在 PR 说明中列出经济影响：

- `js/data/economyConfig.js` — 货币定义、关卡奖励、通天塔结算、广告奖励
- `js/data/constants.js` — 击杀经验基数 (`CULT_KILL_*`)、成长系数
- `js/data/cultivationConfig.js` — 修炼经验公式
- `js/data/tower.js` — 怪物属性膨胀、奖励生成
- `js/engine/runManager.js` — 通天塔结算逻辑 (`settleExp`, `settleAll`)
- `js/views/battle/battleVictoryView.js` — 每层灵石计算

## 改动检查清单

- [ ] 改动后的日灵石产出是否偏离 `dailyTarget` 超过 ±20%？
- [ ] 所有击杀路径是否都正确调用 `addKillExp`？
- [ ] 每层显示值与总结算值是否能对应？
- [ ] 是否存在可被无限刷取的漏洞？
